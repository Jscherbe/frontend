{# {% set sectionOptions = { 
  section: true,
  sectionStartDepth: sectionStartDepth or 1
} %}
{% set sectionMenu = collections.all | navTree(sectionOptions) %} #}

<header class="page__header header">
  <a href="#main" class="site-skip-link hidden-visually-focusable">
    Skip to main content
  </a>
  <a class="header__logo" href="/">
    {{ site.meta.title }}
  </a>
  <nav class="header__nav">
    <h2 class="hidden-visually">Main Menu</h2>
    {{ collections.all | navTree | navTreeActiveTrailOnly | navTreeToHtml | safe }}
  </nav>
  
  {# Links to thinks outside site (github, npm) #}
  {# <div class="header__links">
  </div> #}
</header>