{
  "name": "dist-src",
  "version": "1.0.0",
  "description": "Dist bundling",
  "main": "index.js",
  "scripts": {
    "serve": "webpack serve --mode development --progress --stats minimal --env docs",
    "build": "webpack build --mode production --progress --stats minimal --env docs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ulu/webpack-mixin": "^1.0.17"
  }
}
